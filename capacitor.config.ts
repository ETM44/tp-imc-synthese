import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tp-imc-synthese',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
